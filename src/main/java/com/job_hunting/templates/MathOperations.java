package com.jobhunting.templates;

/**
 * @description: Math类操作
 * @Param:
 * @return:
 * @author: cupxu
 * @create: 2022-04-25 14:29
 **/
public class MathOperations {

    {
        int val = 0;
        int value = 0;
        double dec1 = 1.0;
        double dec2 = 2.0;
        
        Math.abs(val);
        Math.max(val, value);
        Math.min(val, value);
        // 大于或等于dec1 的最小整数
        Math.ceil(dec1);
        // 小于或等于dec1 的最大整数
        Math.floor(dec1);
        // 最接近
        Math.rint(dec1);
        // 返回e的dec1次幂
        Math.exp(dec1);
        // 返回dec1的dec2次方
        Math.pow(dec1, dec2);
        // 返回dec1的平方根
        Math.sqrt(dec1);
        // 返回dec1的立方根
        Math.cbrt(dec1);

    }
}
